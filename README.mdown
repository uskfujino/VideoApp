Sinatra + Mongodb + Bootstrap + Haml のテスト用プロジェクトです。

rvm use 1.9.2

rvm gemset create bootstrap-haml

rvm gemset use bootstrap-haml

gem install sinatra

gem install haml

gem install mongoid

### Sinatraを実行
ruby -rubygems app.rb 

### 確認
http://localhost:4567/
